'use strict';

/**
 * formular-popup controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::formular-popup.formular-popup');
